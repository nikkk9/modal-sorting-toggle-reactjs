import React from "react";
import classes from "./open-toggle.module.css";

const OpenToggle = () => {
  return (
    <div className={classes.toggle}>
      <p>Hello</p>
      <p>Good Morning!</p>
    </div>
  );
};

export default OpenToggle;
